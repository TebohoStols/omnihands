import React from 'react'

const Product = (props) => {

  const {id, name, image, description, slug} = props.data;
  return (
    <div className='bg-white p-5 rounded-xl shadow-sm'>
      
    </div>
  )
}

export default Product
